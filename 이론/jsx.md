HTML템플릿?
## React

### 프레임워크는 아님



### JSX

JS에서 사용하는 HTML 템플릿

### Functional 하다

React의 철학은 functional programming.

inherintance보다는 composition을 쓰려고한다.

### 장단점

virtual dom(랜더링 요소를 메모리에 담아둔다)을 사용해서 state가 변하면 다시 랜더링 할 수 있도록 한다.

자식 Component에서 부모 Component로 state를 전파시킬 수 없다 (그래서 Redux를 쓴다?).

## TypeScript

### Type?

JS의 문제점이었던 Type을 지정하지 않는 문제를 해결할 수 있다. Type을 선언해줘야함. 덕분에 에러를 잡기 쉽다.